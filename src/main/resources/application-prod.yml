spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate



cloudinary:
  application:
    name: portfolio

cors:
  allowed-origins: https://yubraj-portfolio.pages.dev,https://yubrajsahoo.github.io,https://uat.yubraj-portfolio.pages.dev/